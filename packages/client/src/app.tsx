// M4 stub: top-level Ink component. Impl throws — the M4 loop wires it up.

import React from "react";

export type AppProps = {
  canvasId: string;
  userId: string;
  userName: string;
  wsUrl: string;
};

export function App(_props: AppProps): React.ReactElement {
  throw new Error("M4 not implemented: App component");
}
